import {
  ADD_TO_PLAYLIST,
  REMOVE_FROM_PLAYLIST,
  RENAME_PLAYLIST,
  DELETE_PLAYLIST,
} from "./playlistActionTypes";

export const playlistReducer = (state, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
